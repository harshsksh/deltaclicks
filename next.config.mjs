/** @type {import('next').NextConfig} */
const nextConfig = {
  // Use Webpack instead of Turbopack to avoid file watching issues
  // turbopack: false, // This is the default in Next.js 16
};

export default nextConfig;
